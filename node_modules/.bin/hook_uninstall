#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../precommit-hook/bin/uninstall" "$@"
  ret=$?
else 
  node  "$basedir/../precommit-hook/bin/uninstall" "$@"
  ret=$?
fi
exit $ret
