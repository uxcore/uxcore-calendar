import moment from 'moment';

export default class Event {
  constructor(event, options) {
    // this.event = event;
    // this.opts = options;
    // this.top = 0;
    // this.getTop();
  }
  getTop() {
    // let diffStartCurrent = eStart - startCurrent;
    // this.top = diffStartCurrent / totalSeconds;
  }
  getWidth() {}

  getHeight() {}

  getXOffset() {}
}
